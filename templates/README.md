# Capture Templates

- `capture-plan.md` — plan a controlled capture
- `capture.env.example` — example capture environment
- `normalized-timeline.tsv` — normalized timeline structure
- `operator-observation.md` — operator observation form

Copy templates into a private external workspace before recording real account,
device, location, or session data.
