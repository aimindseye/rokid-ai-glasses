# Analysis Helpers

- `analyze_canary_matches.py` — analyze controlled marker matches
- `compare_controlled_captures.py` — compare controlled capture arms
- `export_websocket_occurrences.sh` — export WebSocket occurrences
- `summarize_websocket_traffic.py` — summarize WebSocket traffic

Use these only on evidence you are authorized to inspect. Sanitize outputs
before publication.
