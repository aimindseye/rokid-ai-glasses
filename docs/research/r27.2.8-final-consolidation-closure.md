# R27.2.8 FINAL — Whole-History Consolidation Closure and Next-Test Readiness

R27.2.8 FINAL closes the R27 repository/tooling consolidation program. The purpose of R27 was not to rewrite every historical research script. It was to remove genuine repeated implementations where equivalence could be proven, preserve independent regression oracles, and explicitly disposition superficially similar families that should remain separate.

## Final state

- **88 historical implementations canonicalized** behind stable semantic interfaces or compatibility shims.
- **38 current regression-oracle suites preserved** as independent verification rather than folded into the code they test.
- **4 historical/deferred entries preserved** because they are not current-tree equivalents or require a missing historical fixture.
- **4 residual two-member host-only families preserved as distinct implementations** after similarity/contract review.
- **0 remaining host-only multi-member consolidation candidates**.
- **0 repository path deletions** performed by the R27 consolidation sequence.
- **No device, privileged, or network operation** is performed by this finalization package.

## R27.2.8 reduction

The final two canonicalized implementations are the Test 19 network analyzers:

- `scripts/tests/analyze_test19_network.py`
- `scripts/tests/analyze_test19_r2_network.py`

Their original bytes are archived before replacement. The historical paths remain available as compatibility shims backed by `scripts/research/canonical/network_privacy_analyzer.py`. The final gate repeats all five qualified r1/r2 PASS/FAIL/BLOCKED cases against the archived originals.

## Preserved distinct families

The remaining paired families are intentionally **not** consolidation debt:

1. `validate_r25_run.py` / `validate_r25_1_run.py` — materially different evidence schemas.
2. Test 21 r3.3.3.2/r3.3.3.2.1 offline analyzers — large revision-specific research analyses with low implementation similarity.
3. Test 19/Test 20 r2 event analyzers — different tests and different evidence semantics.
4. Test 21 r3.3.4.2.1/r3.3.4.2.2 closure analyzers — separate closure questions with extremely low implementation similarity.

Their exact SHA-256 identities and rationale are machine-readable in `scripts/research/canonical/profiles/r27-final-consolidation.json`.

## Operator status

```bash
scripts/rokid-research consolidation status
```

A passing repository prints `R27_WHOLE_HISTORY_CONSOLIDATION=COMPLETE` and `NEXT_DEVICE_TEST_READY=YES`.

## Next research test

The next device-facing workstream is **Test 22 — Independent On-Glasses Wi-Fi, Routed IP, and Third-Party Direct Socket Capability**. Its purpose is to determine whether a sideloaded ordinary APK can independently use on-glasses Wi-Fi/IP networking and connect directly to a controlled backend without phone/Bluetooth forwarding making the test falsely pass.
