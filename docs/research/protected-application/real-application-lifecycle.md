# RealApplication Lifecycle

Candidate: `com.rokid.sprite.global.RealApplication`

Status: `STATIC_CANDIDATE_NOT_PRESENT_IN_RUNTIME_CLASS_INVENTORY`

The candidate is absent from both the supplied APK DEX census and the accepted 20,564-class runtime inventory. Constructor, `attachBaseContext`, `Application.attach`, and `onCreate` execution remain unobserved. The prior static candidate label is retained without runtime promotion.
