# r1.3.3.2.25.3.1.3 — exact ADB-toggle frame grammar and field-role closure

## Objective

Recover the bounded application-message grammar from the already accepted
r25.3.1.2 private analysis without contacting the phone or glasses and without
transmitting or replaying any captured payload.

## Required closure

- verify the exact accepted r25.3.1.2 private-analysis ZIP and its manifest;
- prove self-inclusive 32-bit big-endian outer and nested lengths;
- recover a constant field order and constant envelope identifiers;
- establish the one-byte monotonic transaction/sequence candidate;
- prove repeat equality after normalizing only that candidate byte;
- correlate the distinct enable/disable discriminator and structured state with
  the already qualified stock UI and `persist.vendor.adb` observations;
- emit private exact analysis and a separate sanitized publication set.

## Safety boundary

The runner is host-only. It includes no ADB, fastboot, device API, custom RFCOMM
client, payload generation, transmission, or captured-payload replay operation.
Raw message bytes and exact application identifiers remain private.
