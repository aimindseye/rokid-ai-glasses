# r1.3.3.2.25.2.2 — Stock-App-Assisted BLE Identity Correlation, Provisioning-GATT Address Attribution, and Sanitized Endpoint Handoff

## Accepted input

The accepted r25.2.1 capture completed all three phases with no scan failures or connection attempts, but it produced zero qualifying identity candidates. Its private-evidence archive SHA-256 is `ef5760ebe2a7c5bccffca81f5aac8141788d19f807190ab05fc1f8b51bf029bd`.

## Objective

Use one controlled Hi Rokid reconnect to identify the raw BLE address used for the provisioning GATT session, require observation of characteristic `00009301-0000-1000-8000-00805f9b34fb`, correlate the address to a scan-only HMAC token, and emit a private endpoint handoff without exposing the address publicly.

## Safety boundary

The companion build performs scanning only. The stock app is the only connection owner during the assist window. The handoff is data-only; no independent connection is automatically attempted.
