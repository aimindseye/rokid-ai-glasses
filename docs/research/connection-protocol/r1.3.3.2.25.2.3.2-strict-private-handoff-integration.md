# r1.3.3.2.25.2.3.2 — strict private-handoff HCI capture integration

The workflow invokes the existing accepted `run_r1_3_3_2_25_2_2_2.sh`; it does not duplicate or bypass the handoff creator. Ordering is: disabled baseline, strict provisioning, explicit ready and enabled-button attestation, measured logcat interval, one tap, explicit close, HCI bugreport, probe force-stop, strict-runner completion, revocation attestation, and offline DLCI frame census.
